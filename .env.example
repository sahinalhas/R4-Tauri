# =============================================================================
# REHBER360 - ENVIRONMENT CONFIGURATION (TAURI DESKTOP APP)
# =============================================================================
# Copy this file to .env and fill in your actual values
# Never commit .env to version control!
#
# NOTE: This is a Tauri desktop application. Most backend settings are managed
# by Tauri's Rust backend and stored in the app's data directory.
# These environment variables are primarily for development purposes.

# =============================================================================
# APPLICATION MODE
# =============================================================================

# This is a DESKTOP-ONLY application (Tauri)
# Web mode is not supported (no backend available)
# Always use: npm run tauri:dev for development

# =============================================================================
# TAURI CONFIGURATION
# =============================================================================

# Tauri platform (automatically set by Tauri CLI)
# TAURI_PLATFORM=windows
# TAURI_ARCH=x86_64
# TAURI_FAMILY=windows
# TAURI_DEBUG=false

# Database is automatically managed by Tauri backend
# Location: AppData/Roaming/com.rehber360.app/rehber360.db (Windows)
# No manual configuration needed

# =============================================================================
# AI PROVIDER CONFIGURATION (MANAGED BY TAURI)
# =============================================================================
# AI provider settings are managed through the application's Settings UI
# and stored securely in Tauri's Store (AppData directory)
#
# You can configure:
# - OpenAI API Key (for GPT models)
# - Google Gemini API Key (for Gemini models)
# - Ollama Base URL (for local AI - default: http://localhost:11434)
#
# These settings are NOT stored in .env file for security reasons.
# Use the Settings page in the application to configure AI providers.

# =============================================================================
# DEVELOPMENT ONLY
# =============================================================================

# These variables are only used for development/debugging
# Not needed for production desktop builds

# Enable debug mode for development
VITE_DEBUG=false

# Enable Rust backend logging (info | debug | trace)
RUST_LOG=info
